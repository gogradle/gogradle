language: java
cache:
  directories:
    - $HOME/.gradle/caches/
    - $HOME/.gradle/wrapper/
    - $HOME/.gradle/go/
env:
  - GOGS_DIR=/home/travis/gogs
jdk:
  - oraclejdk8
script: ./gradlew check --info --no-daemon
after_success:
  - ./gradlew jacocoTestReport coveralls

before_install: mkdir ~/gogs && git clone https://github.com/gogits/gogs.git ~/gogs

